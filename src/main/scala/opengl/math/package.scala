package net.jakewoods.beatemup.opengl

/** Taken from: https://github.com/jpbetz/subspace
  *
  * We've stolen the source because no 2.12 compatible build is
  * available. We didn't submit a PR because subspace uses gradle
  * which is tricky to configure for cross-builds.
  */
package object math
